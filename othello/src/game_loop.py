import pygame


class GameLoop:
    def __init__(self, board, clock, renderer, event_queue):
        self.board = board
        self.clock = clock
        self.renderer = renderer
        self.event_handler = event_handler

    def start(self):
        while True:
            if self.
